#search-query.properties
searchResumeList=select * from resume where open='Y'
getSuperSearchResume=SELECT * FROM (select c.*, rownum rn from (select * from (select resume.*,title||' '||name||' '||email||' '||school a from resume) b where regexp_like(b.a,?)) c order by 1 DESC) WHERE rn BETWEEN ? AND ?
getSuperSearchNotification=select * from (select b.*,rownum rn from (select n.*,m.nickname,title||' '||p_language||' '||contents||' '||jobs||' '||hope a from notification n, member m where n.co_no=m.m_no) b where regexp_like(b.a,?) and rownum<=? order by noti_no desc) where rn>=?